# legacy/

`donggang-market.html` 是重構前的原始單檔 app（東港華僑市場店家調查專用）。

保留原因：

- 重構期間可並排比對行為（渲染、編號、匯出）
- 是 `src/templates/donggangMarket.ts` 與 `src/model/legacy.ts` 移植邏輯的來源

它**不參與建置**，只是歷史參考。所有功能已移植到 `src/`。
